apiVersion: 1
providers:
  - name: 'MarketPulse Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # PATH inside grafana
      path: /etc/grafana/provisioning/dashboards